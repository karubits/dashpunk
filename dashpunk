#!/usr/bin/env python3
"""Launcher for Dashpunk (runs from the repo checkout)."""

import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), "src"))

from dashpunk.main import main  # noqa: E402

raise SystemExit(main())
